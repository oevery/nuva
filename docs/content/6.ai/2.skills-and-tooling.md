---
title: Skills 与协作约定
description: 在 Nuva 项目里同步 skills，并把 AI 协作约定接到业务开发流程中。
---

本页记录模板项目里内置的 AI 辅助开发能力。先把 skills 管理好，再把它们接到你的业务开发流程里。

## 安装 skills

如果使用模板推荐的 skills，可在安装依赖后同步：

```bash
pnpx skills experimental_install
```

更新 skills：

```bash
pnpx skills update
```

## 模板项目使用

通过 giget 单独下载模板后，先安装依赖，再同步 skills：

```bash
pnpm install
pnpm skills:install
```

## 提交约定

- 提交 `skills.json` 和 `skills-lock.json`。
- 不提交 `.agents/`，它是本地安装产物。
- 如需离线模板或强冻结 skills，再考虑提交 `.agents/`。

## 协作建议

- AI 生成代码后，优先让它落到 `app`、`server` 或 `shared` 的真实结构里。
- 不要把临时性的探索结论长期停留在对话里，应沉淀进项目文档或注释。
- 当某项约定影响业务协作时，优先更新项目内文档。

## 文档优先级

对于 Nuva 这类项目，文档不是补充物，而是业务协作的一部分。一个没有写进文档的约定，本质上仍然是不稳定约定。
